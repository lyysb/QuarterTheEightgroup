<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lyy.quartertheeightgroup">
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.avtivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.avtivity.ImmersiveActivity" />
        <activity android:name=".view.avtivity.RecommendActivity" />
        <activity android:name=".view.avtivity.BaseMvpActivity" />
        <activity android:name=".view.avtivity.EdxitActivity" />
        <activity android:name=".view.avtivity.SayActivity" />
        <activity android:name=".view.avtivity.PhotoActivity" />
        <activity android:name=".view.avtivity.PublishActivity" />
        <activity android:name=".view.avtivity.MyWorkActivity" />
        <activity android:name=".view.avtivity.SetActivity"></activity>
    </application>

</manifest>